/**
 * Root ambient declarations for Kuma.
 *
 * Referenced by tsconfig `files`/`types`. Kept intentionally minimal — the
 * finance domain lives in src/models (Prisma) and src/controllers (endpoints).
 */

export {};

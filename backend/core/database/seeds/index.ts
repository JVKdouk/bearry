// No seed users: authentication is OPAQUE, so accounts can only be created via
// the real registration handshake (which also provisions the zero-knowledge
// vault). Add non-auth seeds here if needed.

async function runner() {
  // intentionally empty
}

void runner();
